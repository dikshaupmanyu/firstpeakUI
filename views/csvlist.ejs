           <%- include partials/header %>
<style type="text/css">
.switch {
  position: relative;
  display: inline-block;
  width: 60px;
  height: 34px;
}

.switch input { 
  opacity: 0;
  width: 0;
  height: 0;
}

.slider {
  position: absolute;
  cursor: pointer;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #ccc;
  -webkit-transition: .4s;
  transition: .4s;
}

.slider:before {
  position: absolute;
  content: "";
  height: 26px;
  width: 26px;
  left: 4px;
  bottom: 4px;
  background-color: white;
  -webkit-transition: .4s;
  transition: .4s;
}

input:checked + .slider {
  background-color: #2196F3;
}

input:focus + .slider {
  box-shadow: 0 0 1px #2196F3;
}

input:checked + .slider:before {
  -webkit-transform: translateX(26px);
  -ms-transform: translateX(26px);
  transform: translateX(26px);
}

/* Rounded sliders */
.slider.round {
  border-radius: 34px;
}

.slider.round:before {
  border-radius: 50%;
}
</style>

       
        <div class="container-fluid">

          <!-- Page Heading -->
         <!--  <h1 class="h3 mb-2 text-gray-800">Tables</h1>
          <p class="mb-4">DataTables is a third party plugin that is used to generate the demo table below. For more information about DataTables, please visit the <a target="_blank" href="https://datatables.net">official DataTables documentation</a>.</p>
 -->
          <!-- DataTales Example -->

      <div class="card shadow mb-4">
           <div class="card-header py-3">
              <h6 class="m-0 font-weight-bold text-primary">CSV List</h6>

             <!--  <h6 class="m-0 font-weight-bold text-primary">CSV List</h6>
              Click the button then choose a file to see its contents displayed below.
                <button onclick="openFile(dispFile)">Open a file</button>
                <pre id="contents"></pre> -->
                <span id="wrap"></span>

                 <div class="card-body">
              <div class="table-responsive">

              <h4>Downloaded CSV</h4>
             <!--  <label class="switch">
                   <input type="checkbox" id="togBtn">

                  <span class="slider round"></span>
              </label>
              <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn").on('change', function() {
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                        // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: 'http://localhost:5555/verifystatus',
                        data: {status: switchStatus , tokenval : tokens},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: 'http://localhost:5555/verifystatus',
                        data: {status: switchStatus, tokenval : tokens},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script> -->
                <table class="table table-bordered" id="dataTables" width="100%" cellspacing="0">
                  <thead>
                    <tr>
                      <th>CSV Name</th>
                      <th>Download Link</th>
                      <th>Sean</th>
                      <th>Bushwick</th>
                   </tr>
                  </thead>
                  <tbody>
                 
                    <tr>
                    <td>words.csv</td>
                    <td>
                      <a href="/csv/words.csv" download>Download CSV File</a>
                    </td>
                    <td> <label class="switch">
                   <input type="checkbox" id="togBtn1" onclick="show_now1();">

                  <span class="slider round"></span>
              </label>
              <div id="display1"></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn1").on('change', function() {
                  // alert("diksha");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    var time = $("#display1").text();
                    // alert(time);
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/Seanwordstatus',
                        data: {status: switchStatus , tokenval : tokens ,currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                            // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/Seanwordstatus',
                        data: {status: switchStatus, tokenval : tokens ,currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                            // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script>
            </td>
              <td>
               <label class="switch">
                   <input type="checkbox" id="togBtn2" onclick="show_now2();">

                  <span class="slider round"></span>
              </label>
              <div id=display2></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn2").on('change', function() {
                  // alert("dik2");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         var time = $("#display2").text();
                         // alert(time);
                        // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/Bushwickwordstatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/Bushwickwordstatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script></td>
                   </tr>
              <tr>
                    <td>Black_list.csv</td>
                    <td><a href="/csv/Black_list.csv" download>Download CSV File</a></td>
                    <td> <label class="switch">
                   <input type="checkbox" id="togBtn3" onclick="show_now3();">

                  <span class="slider round"></span>
              </label>
              <div id=display3></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn3").on('change', function() {
                  // alert("hii");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                     var time = $("#display3").text();
                    // alert(time);
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/seanblackliststatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/seanblackliststatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script></td>
              <td> <label class="switch">
                   <input type="checkbox" id="togBtn4" onclick="show_now4();">

                  <span class="slider round"></span>
              </label>
              <div id=display4></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn4").on('change', function() {
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         var time = $("#display4").text();
                         // alert(time);
                        // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/bushwickBlackStatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/bushwickBlackStatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script>
            </td>
                   </tr>
                   <tr>
                    <td>TT STOCK SPLITS.csv</td>
                    <td>
                      <a href="/csv/TT STOCK SPLITS.csv" download>Download CSV File</a>
                    </td>
                    <td> <label class="switch">
                   <input type="checkbox" id="togBtn5" onclick="show_now5();">

                  <span class="slider round"></span>
              </label>
              <div id="display5"></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn5").on('change', function() {
                  // alert("diksha");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    var time = $("#display5").text();
                    // alert(time);
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/SeanTTSTOCKstatus',
                        data: {status: switchStatus , tokenval : tokens ,currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                            // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/SeanTTSTOCKstatus',
                        data: {status: switchStatus, tokenval : tokens ,currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                            // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script>
            </td>
              <td>
               <label class="switch">
                   <input type="checkbox" id="togBtn6" onclick="show_now6();">

                  <span class="slider round"></span>
              </label>
              <div id=display6></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn6").on('change', function() {
                  // alert("dik2");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         var time = $("#display6").text();
                         // alert(time);
                        // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/BushwickTTSTOCKstatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/BushwickTTSTOCKstatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script></td>
                   </tr>
                     <tr>
                    <td>SocialMedia_words.csv</td>
                    <td><a href="/csv/SocialMedia_words.csv" download>Download CSV File</a></td>
                    <td> <label class="switch">
                   <input type="checkbox" id="togBtn7" onclick="show_now7();">

                  <span class="slider round"></span>
              </label>
              <div id=display7></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn7").on('change', function() {
                  // alert("hii");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                     var time = $("#display7").text();
                    // alert(time);
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/seanSoicalMediastatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/seanSoicalMediastatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script></td>
              <td> <label class="switch">
                   <input type="checkbox" id="togBtn8" onclick="show_now8();">

                  <span class="slider round"></span>
              </label>
              <div id=display8></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn8").on('change', function() {
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         var time = $("#display8").text();
                         // alert(time);
                        // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/bushwickSoicalMediaStatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/bushwickSoicalMediaStatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script>
            </td>
                   </tr>



                    <tr>
                    <td>Black_List_SM.csv</td>
                    <td><a href="/csv/Black_List_SM.csv" download>Download CSV File</a></td>
                    <td> <label class="switch">
                   <input type="checkbox" id="togBtn9" onclick="show_now9();">

                  <span class="slider round"></span>
              </label>
              <div id=display9></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn9").on('change', function() {
                  // alert("hii");
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                     var time = $("#display9").text();
                    // alert(time);
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/seanBlack_List_SMStatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/seanBlack_List_SMStatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script></td>
              <td> <label class="switch">
                   <input type="checkbox" id="togBtn10" onclick="show_now10();">

                  <span class="slider round"></span>
              </label>
              <div id=display10></div>

              <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

              <script type="text/javascript">
                var switchStatus = false;
                $("#togBtn10").on('change', function() {
                   var url_string = window.location.href; 
                    var urls = new URL(url_string);
                    var tokens = urls.searchParams.get("tokendata");
                    if ($(this).is(':checked')) {
                        switchStatus = $(this).is(':checked');
                         var time = $("#display10").text();
                         // alert(time);
                        // alert("HII" + switchStatus);// To verify                     
                      $.ajax({
                        type: 'GET',
                        url: '/bushwickBlack_List_SMStatus',
                        data: {status: switchStatus , tokenval : tokens , currenttime : time},success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
            
                    }
                    else {
                       switchStatus = $(this).is(':checked');
                       // alert("HELLO" + switchStatus);// To verify
                       $.ajax({
                        type: 'GET',
                        url: '/bushwickBlack_List_SMStatus',
                        data: {status: switchStatus, tokenval : tokens , currenttime : time},   
                          success:function(data){   
                           var datakk = JSON.stringify(data);
                           // alert(datakk);
                           // alert("Your OTP is : " + data["Otpdata"]);
                         
                            // alert(JSON.Stringify(html));
                          }
                      });
                    }
                });
              </script>
            </td>
                   </tr>



                  
                  </tbody>
                  
                </table>

                 <h4>Uploaded CSV</h4>

                 <p id="msgdata"></p>

                 <table class="table table-bordered" id="dataTables" width="100%" cellspacing="0">
                  <thead>
                    <tr>
                      <!-- <th>CSV Name</th> -->
                      <th>Upload Words CSV</th>
                      <th>Upload Blacklist CSV</th>
                      <th>Upload TT STOCK SPLITS CSV</th>
                      <th>Upload SoicalMedia CSV</th>
                      <th>Upload Blacklist_SM CSV</th>
                      <!-- <th>Please check the name who download the csv or upload the csv</th> -->
                   </tr>
                  </thead>
                  <tbody>
                    <tr>
                    <!-- <td>Words CSV Uploaded</td> -->
                    <td><form action="/word-upload" method="POST" enctype="multipart/form-data">
                      <input type="file"  name="avatar" required="required">
                      <input type="hidden" name="dataUrl" id="urlvalue">
                      <input type="submit" value="Upload">
                      </form>
                    </td>
                    <td><form action="/blcklist-upload" method="POST" enctype="multipart/form-data">
                      <input type="file"  name="avatar" required="required">
                      <input type="hidden" name="dataUrl" id="urlvalue">
                       <input type="submit" value="Upload">
                       </form>
                    </td>
                     <td><form action="/TTSTOCK-upload" method="POST" enctype="multipart/form-data">
                      <input type="file"  name="avatar" required="required">
                      <input type="hidden" name="dataUrl" id="urlvalue">
                       <input type="submit" value="Upload">
                       </form>
                    </td>
                    <td><form action="/soicalMedia-upload" method="POST" enctype="multipart/form-data">
                      <input type="file"  name="avatar" required="required">
                      <input type="hidden" name="dataUrl" id="urlvalue">
                       <input type="submit" value="Upload">
                       </form>
                    </td>
                     <td><form action="/blacklist_SM-upload" method="POST" enctype="multipart/form-data">
                      <input type="file"  name="avatar" required="required">
                      <input type="hidden" name="dataUrl" id="urlvalue">
                       <input type="submit" value="Upload">
                       </form>
                    </td>
                  
                      <!-- Sean &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<input type="radio" name="checkdata" value="Sean" required="required">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Bushwick &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<input type="radio" name="checkdata" value="Bushwick" required="required">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; -->
                  

                      
                      </tr>

                    <!-- <tr>
                      <p><b>Data (words.csv) Last Uploaded by : <span id="wordvalDataUpadte"></span></b></p>
                    </tr> -->
                    
                  </tbody>
                  
                </table>
               <!--  <table class="table table-bordered" id="dataTables" width="100%" cellspacing="0">
                  <thead>
                    <tr>
                      <th>CSV Name</th>
                      <th>Upload CSV</th>
                      <th>Please check the name who download the csv or upload the csv</th>
                   </tr>
                  </thead>
                  <tbody>
                    <tr>
                    <td>Blacklist CSV Uploaded</td>

                  
                      Sean &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<input type="radio" name="checkdata" value="Sean" required="required">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Bushwick &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<input type="radio" name="checkdata" value="Bushwick" required="required">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;

                      </tr>

                    <tr>
                      <p><b>Data (black_list.csv) Last Uploaded by : <span id="blckvalDataUpadte"></span></b></p>
                    </tr>
                    
                  </tbody>
                  
                </table>
 -->              </div>
            </div>

            </div>
          </div>

        </div>

      </div>
      <!-- End of Main Content -->

                 <%- include partials/footer %>
<script type="text/javascript"> 
function show_now1() { 
  // alert("hii");
var my_time = new Date();
// alert(my_time.toString().substring(0, 25));
var lfckv = document.getElementById("togBtn1").checked;
// alert(lfckv);
 if(lfckv === true){
  // alert("true calling");
 document.getElementById('display1').innerHTML=my_time.toString().substring(0, 25);
}else {
   document.getElementById('display1').innerHTML="";
}

}
function show_now2() { 
var my_time = new Date();
 var lfckv2 = document.getElementById("togBtn2").checked;
 if(lfckv2 == true){
 document.getElementById('display2').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display2').innerHTML="";
}
}
function show_now3() { 
var my_time = new Date();
var lfckv3 = document.getElementById("togBtn3").checked;
if(lfckv3 == true){
 document.getElementById('display3').innerHTML=my_time.toString().substring(0, 25);
 }else{
   document.getElementById('display3').innerHTML="";
}
}
function show_now4() { 
var my_time = new Date();
var lfckv4 = document.getElementById("togBtn4").checked;
if(lfckv4 == true){
 document.getElementById('display4').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display4').innerHTML="";
}
}
function show_now5() { 
var my_time = new Date();
var lfckv5 = document.getElementById("togBtn5").checked;
if(lfckv5 == true){
 document.getElementById('display5').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display5').innerHTML="";
}
}
function show_now6() { 
var my_time = new Date();
var lfckv6 = document.getElementById("togBtn6").checked;
if(lfckv6 == true){
 document.getElementById('display6').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display6').innerHTML="";
}
}
function show_now7() { 
var my_time = new Date();
var lfckv7 = document.getElementById("togBtn7").checked;
if(lfckv7 == true){
 document.getElementById('display7').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display7').innerHTML="";
}
}
function show_now8() { 
var my_time = new Date();
var lfckv8 = document.getElementById("togBtn8").checked;
if(lfckv8 == true){
 document.getElementById('display8').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display8').innerHTML="";
}
}
function show_now9() { 
var my_time = new Date();
var lfckv9 = document.getElementById("togBtn9").checked;
if(lfckv9 == true){
 document.getElementById('display9').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display9').innerHTML="";
}
}
function show_now10() { 
var my_time = new Date();
var lfckv10 = document.getElementById("togBtn10").checked;
if(lfckv10 == true){
 document.getElementById('display10').innerHTML=my_time.toString().substring(0, 25);
}else{
   document.getElementById('display10').innerHTML="";
}
}
</script>

    </div>
    <!-- End of Content Wrapper -->

  </div>
  <!-- End of Page Wrapper -->

  <!-- Scroll to Top Button-->
  <a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
  </a>

  <!-- Logout Modal-->
  <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
          <button class="close" type="button" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
        </div>
        <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
        <div class="modal-footer">
          <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
          <a class="btn btn-primary" href="/">Logout</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap core JavaScript-->
  <script src="vendor/jquery/jquery.min.js"></script>
  <script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

  <!-- Core plugin JavaScript-->
  <script src="vendor/jquery-easing/jquery.easing.min.js"></script>

  <!-- Custom scripts for all pages-->
  <script src="js/sb-admin-2.min.js"></script>

 <!-- Page level plugins -->
<!--   <script src="vendor/datatables/jquery.dataTables.min.js"></script>
  <script src="vendor/datatables/dataTables.bootstrap4.min.js"></script> -->

  <!-- Page level custom scripts -->
<!--   <script src="js/demo/datatables-demo.js"></script>
 --> 
 <script type="text/javascript">
   function dispFile(contents) {
  document.getElementById('contents').innerHTML=contents
}
function clickElem(elem) {
  // Thx user1601638 on Stack Overflow (6/6/2018 - https://stackoverflow.com/questions/13405129/javascript-create-and-save-file )
  var eventMouse = document.createEvent("MouseEvents")
  eventMouse.initMouseEvent("click", true, false, window, 0, 0, 0, 0, 0, false, false, false, false, 0, null)
  elem.dispatchEvent(eventMouse)
}
function openFile(func) {
  readFile = function(e) {
    var file = e.target.files[0];
    if (!file) {
      return;
    }
    var reader = new FileReader();
    reader.onload = function(e) {
      var contents = e.target.result;
      fileInput.func(contents)
      document.body.removeChild(fileInput)
    }
    reader.readAsText(file)
  }
  fileInput = document.createElement("input")
  fileInput.type='file'
  fileInput.style.display='none'
  fileInput.onchange=readFile
  fileInput.func=func
  document.body.appendChild(fileInput)
  clickElem(fileInput)
}
 </script>


                <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/4.1.2/papaparse.js"></script>
 <script type="text/javascript">

  // $(document).ready(function () 
  //   {
  //   $.get("./csv", function(data) 
  //       {
  //         alert(data);
  //           $("#fileNames").append(data);
  //       });
  // });

  window.addEventListener("load", function() {

  var url_string = window.location.href; 
  var urls = new URL(url_string);
  var tokens = urls.searchParams.get("tokendata");
  var msgdata = urls.searchParams.get("msg");
  // alert(msgdata);
  document.getElementById("dashboardLink").href = "/home?tokendata="+tokens;

   document.getElementById("HomeLink").href = "/home?tokendata="+tokens;
  document.getElementById("UserLink").href = "/userlist?tokendata="+tokens;
  // document.getElementById("UserCallLink").href = "/userlist?tokendata="+tokens;
   document.getElementById("CommentLink").href = "/commentlist?tokendata="+tokens;
      document.getElementById("CSVLink").href = "/csvlist?tokendata="+tokens;
      document.getElementById("FlagCallLink").href = "/flaglist?tokendata="+tokens;
      document.getElementById("TipLink").href = "/tipList?tokendata="+tokens;
  


      // $('#urlvalue').val(tokens); 

         document.getElementById("urlvalue").value = tokens; 

 $.getJSON( "json/seanWord.json", function( data ) {

          // alert("value is" + data.currenttime);

     if(data.status == "true"){
        // alert("true");

      document.getElementById("togBtn1").checked = true;
      document.getElementById("display1").innerHTML  = data.currenttime;

     }else {
       // alert("false");

      document.getElementById("togBtn1").checked = false;
      document.getElementById("display1").innerHTML  = "";

     }
  
});

 //////////////////////////////////////////////////////

   $.getJSON( "json/bushwickWord.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn2").checked = true;
      document.getElementById("display2").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn2").checked = false;
      document.getElementById("display2").innerHTML  = "";


     }
  
});

   //////////////////////////////////////////////////////
      

       $.getJSON( "json/seanBlack.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn3").checked = true;
      document.getElementById("display3").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn3").checked = false;
      document.getElementById("display3").innerHTML  = "";


     }
 
});


  ///////////////////////////////////////////////////////////

   $.getJSON( "json/bushwikBlack.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn4").checked = true;
      document.getElementById("display4").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn4").checked = false;
      document.getElementById("display4").innerHTML  = "";


     }

});

   //////////////////////////////////////////////////////

      $.getJSON( "json/seanTTSTOCK.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn5").checked = true;
      document.getElementById("display5").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn5").checked = false;
      document.getElementById("display5").innerHTML  = "";


     }

});




   //////////////////////////////////////////////////////////

    $.getJSON( "json/bushwikTTSTOCK.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn6").checked = true;
      document.getElementById("display6").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn6").checked = false;
      document.getElementById("display6").innerHTML  = "";


     }

});

    
//////////////////////////////////////////////////////


 $.getJSON( "json/seanSoicalMedia.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn7").checked = true;
      document.getElementById("display7").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn7").checked = false;
      document.getElementById("display7").innerHTML  = "";


     }

});


///////////////////////////////////////////////////////////////////


 $.getJSON( "json/bushwickSoicalMedia.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn8").checked = true;
      document.getElementById("display8").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn8").checked = false;
      document.getElementById("display8").innerHTML  = "";


     }

});


///////////////////////////////////////////////////////////////////


    
//////////////////////////////////////////////////////


 $.getJSON( "json/seanBlackListSM.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn9").checked = true;
      document.getElementById("display9").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn9").checked = false;
      document.getElementById("display9").innerHTML  = "";


     }

});


///////////////////////////////////////////////////////////////////


 $.getJSON( "json/bushwickBlackListSM.json", function( data ) {

       // alert(data.status);

     if(data.status == "true"){
      // alert("true");

      document.getElementById("togBtn10").checked = true;
      document.getElementById("display10").innerHTML  = data.currenttime;


     }else {
      // alert("false");

      document.getElementById("togBtn10").checked = false;
      document.getElementById("display10").innerHTML  = "";


     }

});


///////////////////////////////////////////////////////////////////


 
 if(msgdata == null){

  document.getElementById("msgdata").textContent = "";

 }else{

  document.getElementById("msgdata").textContent = "File Uploaded Sucessfully !!";

 }


////////////////////////////////////////////////////////////

// const url = "https://apis.tradetipsapp.com/opt/sector-sentiment-files";
// fetch(url, {
//   method: "GET",
//   headers: {
//           Authorization: 'Bearer ' + tokens,
//         },
// })
//   .then(resp => resp.json())
//     .then(function(data) {
//        var datak = JSON.stringify(data);
//        alert(datak)
//      });

// $.getJSON( "json/blcklistdetail.json", function( data ) {

//     // alert(data.name);

//     document.getElementById("blckvalDataUpadte").textContent = data.name;

  
// });


});

</script>

</body>

</html>
