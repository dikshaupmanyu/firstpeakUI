#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  --harmony "$basedir/../ses/bin/ses" "$@"
  ret=$?
else 
  node  --harmony "$basedir/../ses/bin/ses" "$@"
  ret=$?
fi
exit $ret
